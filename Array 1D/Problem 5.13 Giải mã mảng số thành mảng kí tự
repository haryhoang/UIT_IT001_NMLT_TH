// Mô tả bài toán: Viết chương trình để giải mã từ dãy số thành dãy ký tự theo qui ước
chuyển đổi từ số thành ký tự trong bảng mã ASSCII như sau:

VD1: dãy số {3,97} --> "aaa"
Giải thích dãy số {3,97} --> '97' là chuyển đổi của 'a' và '3' là do có 3 ký tự a liền kề nhau.

VD2: dãy số {2,98,2,97} --> "aabb"
Giải thích dãy số {2,98,2,97} --> gồm (2,98) và (2,97) trong đó:
(2,98) --> '2' là do có 2 ký tự 'b' liền kề nhau và '98' là chuyển đổi của 'b' 
(2,97) --> '2' là do có 2 ký tự 'a' liền kề nhau và '97' là chuyển đổi của 'a'
(Lưu ý xuất ngược từ cuối chuỗi lên đầu chuỗi)

Test case
Input  2 99 2 98 2 97 -1
Output aabbcc

Input 1 99 1 98 1 97 1 99 1 98 1 97 -1
Output abcabc
// Logic code: Đầu tien ở mảng nhập mình dùng while để nhập a[i] cho tới khi gặp -1 thì dừng
còn ở trong hàm ouput mình thực hiện chức năng thì mình duyệt từ n-1, tùy theo cách các ban thu 
thập n, mình thu thập n từ 0 nên đủ tới index của n, do vavạy mình cho chjay tới i-1>=0 mục
đích để không truy cập quá mảng, còn lại là logic in khá cơ bản nha.
// Code tham khảo

#include <iostream>
#include <string.h>
using namespace std;
#define MAX 1000

void input(int a[], int &i)
{
    cin >> a[0];
    i = 0;
    while( a[i]!= -1) // while để lặp chính xác số index mình cần
    {
        i++;
        cin >> a[i];
    }
}

void output(int a[], int n)
{

    for (int i=n-1; i-1>=0 ;i-- ) // In mảng ngược nha
    {
        for (int j =0; j<a[i-1]; j++)
        {
            cout << (char)a[i]; // In bình thường theo số lượg chữ cái cần in
        }
        i--;
    }

}

int main()
{
    int n;
    int a[MAX];
    input(a, n);
    output(a, n);
}
