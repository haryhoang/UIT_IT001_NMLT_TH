// Mô tả bài toán: 
Nhập mảng 1 chiều gồm N phần tử số nguyên int.
In ra các phần tử xuất hiện nhiều nhất trong mảng.
Nếu tất cả các phần tử đều xuất hiện 1 lần thì xuất ra màn hình "No Find"
Test case
Input 8 
12 4 23 4 12 3 6 0
Ouput 
12 4

// Logic code: Bài này logic khá đơn giản, nưng mình xử í có vẻ hơi cồng kềnh, đại loiạ tư
duy của mình muốn xác định số phần tử xuất hiện nhiều nhất trong mảng, sau đó lại
duyệt lại lần nữa các phần tử xuất hiện đúng bằng cái maxx thì in ra thui
*Note: Nhớ đièu kiện in No Find là gì, trong bài mình có dùng maxx = 0.5 khá lẻ để đảm bảo mảng 
nhập vào là số nguyên nên không bao giừo bị trùng với maxx mình nhập, đảm bảo logic 
maxx của mình luôn đúng, ngoài ra bạn có thể dùng logic khác là tìm maxx bằng cách đặt maxx = 
a[0] cách này khá hiệu quả nhưng mình thấy hcỉ dùng trong 1 số trường hợp liên quan đến mảng 
nen mình học logic kia có vẻ hay hơn
-> logic code dưới đây là bản mới, bản cũ của mình logic cũng tương tự và đã AC thành công rùi 
nha
// Code tham khảo

#include <iostream>
#include <math.h>
#define Max 1000
using namespace std;

void Input(int a[], int &n)
{
    cin >> n;
    for (int i=0; i<n; i++)
        cin >> a[i];
}

void Fun(int a[], int n)
{

    int cnt = 1;
    int flag =0;
    float maxx = 0.5;
    for (int i=0; i<n; i++)
    {
        for (int j=i+1; j<n; j++)
        {
            if ( a[i] == a[j])
                cnt++;
        }
        if (maxx == 0.5 || cnt > maxx)
            maxx = cnt;
        cnt = 1;
    }
    for (int i=0; i<n; i++)
    {
        for (int j=i+1; j<n; j++)
        {
            if ( a[i] == a[j])
                cnt++;
        }
        if ( cnt == maxx && maxx != 1 )
            {
                cout << a[i] << " ";
                flag++;
            }
        cnt = 1;
    }
    if ( flag == 0)
        cout << "No Find";
    return ;
}

int main()
{
    int a[Max];
    int n;
    Input(a,n);
    Fun(a,n);
    return 0;
}
