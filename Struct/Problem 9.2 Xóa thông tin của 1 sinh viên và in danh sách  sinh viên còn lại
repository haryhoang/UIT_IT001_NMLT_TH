// Mô tả bài toán: Nhập tên một Sinh viên cần xóa trong danh sách, sau đó in ra danh sách các
sinh viên còn lại ( Các sinh viên không bị xóa tên )
Input
Nguyen Thi Lan
3
07520504
Nguyen Thi Lan
31/07/1989
M
10
9.5
10
07520045
Nguyen Thi Lan
27/05/1989
F
10
9
8
07521111
Nguyen Anh Hong
07/12/1991
F
7
9
8
Ouput 
07521111	Nguyen Anh Hong	07/12/1991	F	7	9	8	8
// Code tham khảo

#include <iomanip>
#include <iostream>
#include <stdio.h>
#include <string.h>
using namespace std;
#define MAXN 100

struct SinhVien {
    char MASV[10];
    char HoTen[100];
    char NgaySinh[12];
    char GioiTinh;
    float DiemToan, DiemLy, DiemHoa, DTB;
};

void Nhap(char hoTenCanXoa[])
{
    cin.getline(hoTenCanXoa, 100);
}
void Nhap_Sinhvien(SinhVien &A)
{
    cin.getline(A.MASV, 10);
    cin.getline(A.HoTen, 100);
    cin.getline(A.NgaySinh, 12);
    cin >> A.GioiTinh;
    cin >> A.DiemToan >> A.DiemLy >>  A.DiemHoa;
    cin.ignore();
}

void Nhap(SinhVien A[], int &n)
{
    cin >> n;
    cin.ignore();
    for (int i=0; i<n; i++)
    {
        Nhap_Sinhvien(A[i]);
    }
    return ;
}

void XoaTheoHoTen(SinhVien A[], int &n,char hoTenCanXoa[])
{
    for (int i=0; i<n; i++)
    {
        if ( strcmp(A[i].HoTen, hoTenCanXoa) == 0 ) // Sử dụng hàm strcmp để so sánh chuỗi và xóa
        {
            for (int j=i; j<n; j++)
            {
                A[j] = A[j+1];
            }
            n--; i--;
        }
    }
}

void Xuat_Sinhvien(SinhVien A)
{
    cout << A.MASV << "\t" << A.HoTen << "\t" << A.NgaySinh << "\t";
    cout << A.GioiTinh << "\t" << A.DiemToan << "\t" << A.DiemLy << "\t";
    cout << A.DiemHoa << "\t";
    cout.precision(3); cout <<  (A.DiemToan + A.DiemHoa + A.DiemLy)/3.0;
}

void Xuat(SinhVien A[], int n)
{
    for (int i=0; i<n; i++)
    {
        Xuat_Sinhvien(A[i]);
        cout << endl;
    }
    return;
}


int main() { // template bắt buộc tuân theo
    SinhVien A[MAXN];
    char hoTenCanXoa[100];
    int n;

    Nhap(hoTenCanXoa);

    Nhap(A, n);

    XoaTheoHoTen(A, n, hoTenCanXoa);

    Xuat(A, n);
    return 0;
}
