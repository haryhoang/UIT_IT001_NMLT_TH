// Mô tả bài toán: Sử dụng đệ quy giải quyết bài toán: Nhập một số n với điều kiện 
0<n<=50, nếu không thỏa điều kiện thì nhập lại, sau đó tính tổng của n số nguyên tố tương ứng,
Test case
Input 5
Ouput 28
Giải thích: n=5 thỏa điều kiện nên sum = 2+3+5+7+11  28.
// Logic code: Ở bài toán này, mình sẽ vận dụng tư duy đệ quy như thế nào, đầu tiên chia ra làm 3 
hàm con, hàm input, hàm fun và hàm snt bổ trợ, ở hàm nhập thì mình tư duy rằng trước tiên ta phải n
nhập n đúng không, sau đó mình sẽ có điều kiện dừng, nếu thỏa thì dừng, còn nếu không thỏa thì mình gọi
lại vòng đệ quy, các bạn cứ tưởng tượng mình muốn chương trình chạy như thế nào thì tuw duy code ra y 
vậy, đến với hàm fun thì mình dùng 4 tham số, lần lượt là n, s, cnt và i trong đó cnt là biến đếm 
dùng để cập nhật xem đã bằng n chưa thì sẽ dừng, biến s dùng để sum các i thỏa mãn, mình cho i chạy
thoải mái cho đến khi điều kiện thỏa là được, dùn đệ quy đuôi cho mấy bài này cực kì hiệu quả nên 
các bạn tập đệ quy đuôi cho quen nhé.
// Code tham khảo

#include<iostream>
using namespace std;
#define MAX 100

void input(int &n)
{
    cin >> n;
    if ( n<=0 || n>50)
        return input(n); // nếu chưa thỏa thì mình sẽ cho chjay lại ( thực hiện ctrìn lại từ đầu )
    return; // Còn thỏa rùi thì return -> exist
}
int snt(int n, int i)
{
    if ( n<=1 )
        return 0;
    if (i*i>n) return 1; // Tư duy lần lượt các điều kiện giống với hàm for nhá, trước tiên kiểm tra
    // xem n<=1 hay không rồi tới liệu i*i<=n không, logic float vậy mới chính xác nha
    if ( n%i == 0) return 0;
    return snt(n, i+1);
}

int fun(int n, int s, int cnt, int i)
{
    if ( cnt==n) return s; // Khi này là các giá trị snt đã đủ nên dừng nhe
    if ( snt(i, 2) == 1 )
    {
        s+=i;
        cnt++;
    }
    return fun(n, s, cnt, i+1 ); // Dùng đệ quy đuôi để tăng i
}

int main(){
    int n;
    input(n);
    cout << fun(n, 0, 0, 2);
}
